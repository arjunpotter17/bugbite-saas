{
  "name": "bugbite",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny paid-access SaaS that turns one messy customer complaint into one clean bug ticket.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "vite": "^7.2.7",
    "typescript": "^5.9.3"
  },
  "devDependencies": {}
}
